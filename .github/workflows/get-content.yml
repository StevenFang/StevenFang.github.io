name: 定时更新
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  job1:
    name: 获取网页内容
    runs-on: ubuntu-latest
    steps:
      - name: 安装 curl
        run: sudo apt-get update && sudo apt-get install -y curl
      - name: 获取网页内容
        run: curl -s "http://xhww.fun:63/小米/DEMO.json" > ./1.json
